{
  "name": "minervawikins_status",
  "description": "Status för minervawikin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "statusfy dev",
    "build": "statusfy build",
    "start": "statusfy start",
    "generate": "statusfy generate",
    "new-incident": "statusfy new-incident",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "statusfy": "^0.1.2"
  },
  "engines": {
    "node": ">=8.10",
    "npm": ">=5"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
